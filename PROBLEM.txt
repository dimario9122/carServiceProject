Проблема
@app.route('/record', methods=['GET', 'POST'])
def record():

при выборе марки/модели/типа услуг стоимость должна меняться,
а функция срабатывает только при ['GET', 'POST'] запросах, 
то есть при изменении выбора функция record() никак не реагирует
